# App
PORT=3000
CORS_ORIGIN=*

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres123
DB_NAME=learning-1
DB_SYNCHRONIZE=true

# JWT
JWT_SECRET=change_me_in_production
JWT_EXPIRES_IN=1d
